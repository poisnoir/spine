// Server accepts incoming connections and handles them.
// In future, A method of encryption will be added but for beta we will assume safe networks.

// server shall have the list of current namespaces from spined
// On connection, server shall exchange its common namespaces with the client and then
// send the list of nodes and entities it has available to the client. then
const server = struct {};
